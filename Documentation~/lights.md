# Lights

`InfinityAdditionalLightData` stores Infinity-only light fields (layers, contact, volumetric, distance fade). Unity Light remains the owner of color, intensity, type, and shadows. Disc lights show a warning and are not substituted.
